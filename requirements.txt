# === Core LLM Orchestration ===
langchain==1.3.11
langchain-community==0.4.2
langchain-classic          # EnsembleRetriever, BM25Retriever 등 langchain 1.x 분리 모듈
langgraph                  # 조건부 Edge 라우팅 Agent 그래프

# === LLM Backend (교체 가능 구조) ===
langchain-anthropic        # 공개 데모용 API 모델 (Claude)
langchain-ollama           # 로컬 sLLM 교체용 (Ollama 기반)

# === Retrieval: Hybrid Search ===
langchain-chroma           # Dense vector store
chromadb
langchain-huggingface      # Sentence-Transformers 임베딩 래퍼
sentence-transformers
rank_bm25                  # Sparse (BM25) retriever

# === Observability ===
langsmith                  # 트레이싱 및 평가

# === 웨이퍼 CNN Tool (wafer_detection6.ipynb / streamlit_wafer.py 와 동일 스택) ===
tensorflow-cpu     # Streamlit Cloud 등 CPU 배포 환경에서는 tensorflow-cpu 권장 (GPU 있으면 tensorflow로 교체)
opencv-python-headless
joblib

# === App / Deploy ===
streamlit
python-dotenv
pydantic>=2.0
